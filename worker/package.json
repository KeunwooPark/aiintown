{
  "name": "aiintown-ask",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that answers questions about AI in Town events using Claude.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/strip-markdown.test.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "marked": "^18.0.6"
  }
}
